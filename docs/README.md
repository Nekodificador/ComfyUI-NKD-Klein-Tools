# docs/

Images referenced by the main README.

- `reference_weight_example.png` — screenshot of the Reference Weight node migrating
  the lighting from a second reference onto the base portrait (the "migrate the light
  from image 2, the teeth shine" example). Drop the screenshot here with this exact
  filename so the README image renders.
